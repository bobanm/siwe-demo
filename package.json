{
    "name": "siwe-demo",
    "version": "0.0.0",
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "lint": "oxlint",
        "dev": "bun run --cwd apps/api dev",
        "build": "bun run --cwd apps/web build",
        "deploy": "bun run --cwd apps/api wrangler deploy"
    },
    "devDependencies": {
        "@types/bun": "^1.3.14",
        "oxlint": "^1.64.0",
        "typescript": "5.9.3"
    }
}